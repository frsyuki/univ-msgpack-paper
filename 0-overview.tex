
今日では、プログラムを同時並行的に複数のサーバ上で実行し、ネットワークを介して互いにメッセージを交換しあうことで構成される分散システムが、幅広く利用されている。それらのシステムは、スクリプト言語を含む様々なプログラミング言語で実装されたプログラムが、新旧入り混じった様々なサーバ上で実行され、ソフトウェアおよびハードウェアのアップデートを繰り返しながら長期間運用される。分散システムは、このような異種混合の環境においても相互運用性を維持し、動作し続けなければならない。
%このため、異種のサーバ・異種のプログラミング言語・異種のバージョン間で互換性を維持しながら利用可能であり、高い相互運用性を実現するメッセージ表現手法が必要とされている。
%この課題に対して従来から様々な研究と実装がなされてきたが、今日においても分散システムの実装は容易ではない。

そこで本研究では、分散システムのための新たなメッセージ表現手法を設計し実装した。これをMessagePackと命名する。MessagePackは、特定のプログラミング言語に依存しない中立的な型システムと、その型とプログラミング言語の型との相互変換を行う機構を導入することによって、異種のサーバ・異種のプログラミング言語・異種のバージョンのプログラムの間で互換性を維持しながら、メッセージを交換することを可能にした。
さらに、コンパクトに型情報を格納するバイナリ形式の表現形式を設計することによって、優れた空間効率を達成した。また、実装においても最適化を行うことによって、既存のオブジェクトシリアライズ手法と比較して、優れた処理速度を実現した。

本稿ではMessagePackの設計と実装について述べ、その評価を行う。
%MessagePackは、20以上のプログラミング言語を横断して、相互にメッセージを交換することを可能にした。
%MessagePackはソフトウェアライブラリとして使いやすいAPIを提供し、
%分散システム特有の問題の解決を容易にした。



%異種のサーバ・異種のプログラミング言語間で利用可能であり、高い相互運用性を実現するメッセージ表現手法を設計し、実装した。これをMessagePackと命名する。MessagePackはソフトウェアライブラリとして使いやすいAPIを提供し、分散システム特有の課題を容易に解決できるようにする。本稿ではMessagePackの設計と実装について述べ、その後その性能評価を行う。
%
%分散ステム特有の課題を容易に解決できるようにする。
%ようとする試みは数少なく、
%まず高速に動作し、多言語間で利用できるシリアライズ形式を設計し、実装した。これをMessagePackと命名する。次に、MessagePackをプロトコルに用いたRPCシステムを設計し、実装した。これをMessagePack-RPCと命名する。本稿ではMessagePackとMessagePack-RPCの設計と実装について述べ、その後その性能評価を行う。
%
%必ずしも使い勝手
%プログラミングコストの増大を招いている。
%必ずしも下がっていない。
%分散システムの生産性は必ずしも
%
%このため、様々な計算機アーキテクチャやプログラミング言語の混在を可能にする高い相互運用性と、
%を実現するための、メッセージ表現手法が必要とされている。
%
%かつ効率的に動作する
%
%それらのシステムでは、それらのシステムでは、スクリプト言語を含む様々なプログラミング言語で実装されたプログラムの間で、頻繁にデータのやりとりが行われる。また、それらのシステムは数多くの計算機を用いて構成されることも多く、システム全体に占めるメッセージ交換の負荷の割合は高い。このため、
%
%ネットワークを介して
%ことから
%相互互換性
%
%普遍的に利用される
%
%このため、複数の言語の間で、低いCPU負荷と少ない通信量でメッセージを交換できる手法が求められている。
%
%今日では多種多様なサービスがインターネットを通じて提供されている。それらのシステムでは、スクリプト言語を含む様々なプログラミング言語で実装されたプログラムの間で、頻繁にデータのやりとりが行われる。また、それらのシステムは数多くの計算機を用いて構成されることも多く、システム全体に占めるメッセージ交換の負荷の割合が高い。このため、複数の言語の間で、低いCPU負荷と少ない通信量でメッセージを交換できる手法が求められている。
%そこで本研究では、まず高速に動作し、多言語間で利用できるシリアライズ形式を設計し、実装した。これをMessagePackと命名する。次に、MessagePackをプロトコルに用いたRPCシステムを設計し、実装した。これをMessagePack-RPCと命名する。本稿ではMessagePackとMessagePack-RPCの設計と実装について述べ、その後その性能評価を行う。

